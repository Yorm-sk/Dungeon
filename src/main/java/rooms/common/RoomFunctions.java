package rooms.common;

/**
 * Functions that every room will have
 */
public interface RoomFunctions {
    /**
     * describes player a room with text;
     */
    void showDescription(String text);

}
